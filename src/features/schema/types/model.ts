/**
 * refers to the ModelStep table in the backend
 * this enum are the possible `type` values in the table
 */
export enum ModelStepType {
  mentions = 'MENTIONS',
  entities = 'ENTITIES',
  relations = 'RELATIONS',
}
